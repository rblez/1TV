# Copia este archivo a .env.local y llena los valores reales (nunca se commitean)

# Supabase — dashboard > Project Settings > API
PUBLIC_SUPABASE_URL=https://mpvknreeayjpmsbbweqn.supabase.co
PUBLIC_SUPABASE_ANON_KEY=tu_anon_key

# Vimeus — API Key desde tu dashboard (solo servidor, nunca exponer)
VIMEUS_API_KEY=tu_api_key

# Vimeus — View Key desde /settings/general (va en los embed URLs)
PUBLIC_VIMEUS_VIEW_KEY=kxH8p5KB2hZxm7rdGrGU-Z69Shhy7kC3_nKrJw9jsw

# Cron — secreto para /api/admin/sync (Vercel Cron lo envía como Authorization: Bearer $CRON_SECRET)
CRON_SECRET=genera_un_random_de_32_chars